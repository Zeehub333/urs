// buttons/DropdownButton5 — Material DropdownButton distinct v5
// unique: disabled — hash 0206
export class DropdownButton5 {
  constructor(props){ this.props=props||{}; }
  render(){ const p=this.props; var label=p.label||'DropdownButton5'; return '<button class="md-btn md-btn-primary dropdownbutton5" disabled style="opacity:.5;cursor:not-allowed">'+label+' — disabled (0206f8)</button>'; }
}