// buttons/DangerButton5 — Material DangerButton distinct v5
// unique: disabled — hash a199
export class DangerButton5 {
  constructor(props){ this.props=props||{}; }
  render(){ const p=this.props; var label=p.label||'DangerButton5'; return '<button class="md-btn md-btn-primary dangerbutton5" disabled style="opacity:.5;cursor:not-allowed">'+label+' — disabled (a199f8)</button>'; }
}