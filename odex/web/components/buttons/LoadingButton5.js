// buttons/LoadingButton5 — Material LoadingButton distinct v5
// unique: disabled — hash 06cd
export class LoadingButton5 {
  constructor(props){ this.props=props||{}; }
  render(){ const p=this.props; var label=p.label||'LoadingButton5'; return '<button class="md-btn md-btn-primary loadingbutton5" disabled style="opacity:.5;cursor:not-allowed">'+label+' — disabled (06cde0)</button>'; }
}