// buttons/IconButton5 — Material IconButton distinct v5
// unique: disabled — hash 9ca9
export class IconButton5 {
  constructor(props){ this.props=props||{}; }
  render(){ const p=this.props; var label=p.label||'IconButton5'; return '<button class="md-btn md-btn-primary iconbutton5" disabled style="opacity:.5;cursor:not-allowed">'+label+' — disabled (9ca95f)</button>'; }
}