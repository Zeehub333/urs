// buttons/ToggleButton5 — Material ToggleButton distinct v5
// unique: disabled — hash 7603
export class ToggleButton5 {
  constructor(props){ this.props=props||{}; }
  render(){ const p=this.props; var label=p.label||'ToggleButton5'; return '<button class="md-btn md-btn-primary togglebutton5" disabled style="opacity:.5;cursor:not-allowed">'+label+' — disabled (76037d)</button>'; }
}