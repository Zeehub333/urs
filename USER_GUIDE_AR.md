# دليل المستخدم — نظام URS/Odex

> دليل عملي بالعربية للمستخدم النهائي. آخر تحديث: 2026-09-12

---

## 1. التنقل الأساسي

| الصفحة | الوظيفة |
|---|---|
| لوحات المعلومات | مخططاتك المحفوظة — أول صفحة في القائمة |
| تقاريري | التقارير المميزة بنجمة ☆ |
| مستنداتي | نسخ HTML محفوظة من تقاريرك (مشفرة) |
| التطبيقات | الادخالات والتقارير حسب النظام (الموارد البشرية، المبيعات...) |
| الإعدادات | الشركات/الفروع/المستخدمون/الصلاحيات |
| الاتصالات | مصادر البيانات (قواعد بيانات، أجهزة بصمة) |

- زر **رجوع** يعيدك للصفحة السابقة تلقائيًا.
- نقطة **حالة الاتصال** في الشريط العلوي: خضراء = متصل، حمراء = تعذر الوصول.

## 2. تشغيل التقارير

1. افتح التطبيق ← **عرض التقرير**.
2. **البحث الذكي**: اكتب رقمًا (يبحث رقميًا فقط)، نصًا (نصيًا فقط)، تاريخًا (`11092026`)، نطاقًا (`01092026-11092026`)، وقتًا (`09:00`) — افصل البدائل بفاصلة `,` واجمع الشروط بـ `&`.
3. **إخفاء/إظهار الأعمدة**: من قائمة الأعمدة — يُحترم في الطباعة والتصدير.
4. **المفضلة**: زر النجمة ☆ يضيف التقرير إلى صفحة **تقاريري**.

### التصدير والطباعة
- **Excel**: تنزيل مباشر بلا حد للصفوف (مع شريط تقدم).
- **PDF**: من نافذة الطباعة — عربي كامل.
- **طباعة**: معاينة بانل ثم طباعة؛ زر **حفظ نسخة** يخزن HTML في **مستنداتي**.

## 3. تقاريري (المفضلة)

- النجمة ☆ في أي بطاقة تقرير تضيفه هنا.
- كل بطاقة تعرض مخططًا مصغرًا إن وُجد.
- العرض بجريد 3 أعمدة؛ الإزالة بالنجمة مرة أخرى.

## 4. لوحات المعلومات

1. افتح **لوحات المعلومات** ← **＋ إضافة مخطط**.
2. اختر من الشجرة: التطبيق ← التقرير ← المخطط، أو أنشئ **مخططًا مخصصًا** من أعمدة التقرير (نوع المخطط + العمود الرقمي + التصنيف).
3. المخططات محفوظة في متصفحك؛ الحذف من زر البطاقة.

## 5. مستنداتي

- **الحفظ**: من نافذة طباعة أي تقرير ← **حفظ نسخة** (تُحفظ نسخة HTML مشفرة + صورة مصغرة A4).
- **عرض**: يفتح المستند في عارض داخلي.
- **تنزيل**: يحفظ ملف HTML على جهازك.
- **طباعة**: تطبع النسخة المحفوظة مباشرة.
- **حذف**: يحذف النسخة والمصغرة نهائيًا.
- **توقيع واعتماد / إرسال**: قريبًا (بشارة `قريبًا` على الزر).

> الملفات مشفرة ولا تُفتح إلا من النظام (أو أداة `open_doc.py` بكلمة مرور النظام).

## 6. الادخالات (النماذج)

- جدول قابل للبحث والفرز والتصفية + عرض بطاقات.
- إضافة/تعديل/حذف من الشريط العلوي؛ الاستيراد CSV؛ التصدير والطباعة والمشاركة.
- الحقول المحسوبة والمرتبطة تُملأ تلقائيًا.

## 7. الإعدادات

- صفحات مصنفة (النظام/التطبيقات): الشركات، الفروع، المستخدمون، الصلاحيات.
- القواعد تُطبق تلقائيًا خلفيًا (إلزامي/فريد/صيغة/حدود) برسائل عربية.

## 8. الاتصالات ومصادر البيانات

- **قواعد البيانات**: postgres/oracle/sqlserver/mysql — اختبار اتصال + استكشاف جداول وأعمدة.
- **أجهزة البصمة (IoT)**: اتصال نوع `iot` بقائمة أجهزة `IP:Port` — تُنشأ **مرآة** محلية تُزامن تلقائيًا وتُبنى عليها تقارير الحضور (الدخول/الخروج محسوبان بقاعدة `fp_calc`: بداية/نهاية الدوام، عبور منتصف الليل، تجاهل البصمات المتقاربة).

## 9. قواعد الأعمال (للمتقدمين)

- القاعدة تحوّل الشروط إلى قيم داخل التقرير (مثال: حالة الدوام من بصمات الدخول/الخروج).
- كل قاعدة لها **سياسات** مرتبة بالأولوية (الأصغر يفوز) + سياسة **افتراضية** عند عدم تحقق أي شرط.
- تُدار من الشريط الجانبي لصفحة التطبيق (القواعد ← السياسات).

## 10. أسئلة شائعة

- **الأرقام تظهر كأكواد؟** حدّث الصفحة `Ctrl+F5` بعد أي تحديث للنظام.
- **تقرير لا يعمل؟** انسخ رسالة الخطأ من النافذة (زر النسخ) وأرسلها للدعم.
- **نسيت أين حفظت؟** تقاريري (☆) للتقارير، لوحاتي للمخططات، مستنداتي للنسخ المحفوظة.
